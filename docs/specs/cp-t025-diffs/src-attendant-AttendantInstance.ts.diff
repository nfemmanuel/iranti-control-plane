--- a/src/attendant/AttendantInstance.ts
+++ b/src/attendant/AttendantInstance.ts
@@ -1,6 +1,7 @@
 import { db } from '../lib/db';
 import { buildWorkingMemory } from './workingMemory';
 import { buildBrief } from './brief';
+import { getStaffEventEmitter } from '../lib/staffEventRegistry';
 import type { Brief, AttendContext, ObserveResult } from '../types';

 export class AttendantInstance {
@@ -28,6 +29,15 @@ export class AttendantInstance {
     this.brief = await buildBrief(this.agentId, context, this.db);
     this.sessionStarted = new Date().toISOString();
+    getStaffEventEmitter().emit({
+      staffComponent: 'Attendant',
+      actionType: 'handshake_completed',
+      agentId: this.agentId,
+      source: 'internal', // Source not threaded to AttendantInstance in this PR; follow-up required
+      reason: null,
+      level: 'debug',
+      metadata: {
+        briefSize: this.brief?.workingMemory.length ?? 0,
+        taskSummary: context.task.slice(0, 120),
+        sessionId: this.sessionStarted,
+      },
+    });
     return this.brief;
   }

@@ -62,6 +72,15 @@ export class AttendantInstance {
     this.brief = await buildBrief(this.agentId, context, this.db);
     this.contextCallCount = 0;
+    getStaffEventEmitter().emit({
+      staffComponent: 'Attendant',
+      actionType: 'reconvene_completed',
+      agentId: this.agentId,
+      source: 'internal',
+      reason: null,
+      level: 'audit',
+      metadata: {
+        briefSize: this.brief?.workingMemory.length ?? 0,
+        sessionId: this.sessionStarted,
+        contextCallCount: this.contextCallCount,
+      },
+    });
     return this.brief;
   }

@@ -98,6 +117,14 @@ export class AttendantInstance {
     this.contextCallCount++;
     const result = await this.runAttend(input);
+    getStaffEventEmitter().emit({
+      staffComponent: 'Attendant',
+      actionType: 'attend_completed',
+      agentId: this.agentId,
+      source: 'internal',
+      reason: null,
+      level: 'debug',
+      metadata: {
+        contextCallCount: this.contextCallCount,
+        sessionId: this.sessionStarted,
+      },
+    });
     return result;
   }

@@ -140,6 +167,14 @@ export class AttendantInstance {
   async observe(input: ObserveInput): Promise<ObserveResult> {
     const result = await this.runObserve(input);
+    getStaffEventEmitter().emit({
+      staffComponent: 'Attendant',
+      actionType: 'observe_completed',
+      agentId: this.agentId,
+      source: 'internal',
+      reason: null,
+      level: 'debug',
+      metadata: {
+        observeType: input.type ?? null,
+        sessionId: this.sessionStarted,
+      },
+    });
     return result;
   }

@@ -178,6 +213,16 @@ export class AttendantInstance {
   onContextLow(): void {
     // Context window nearing limit — archive current session and mark expired
     this.archiveSession();
+    getStaffEventEmitter().emit({
+      staffComponent: 'Attendant',
+      actionType: 'session_expired',
+      agentId: this.agentId,
+      source: 'internal',
+      reason: 'Context window threshold reached. Session archived.',
+      level: 'audit',
+      metadata: {
+        sessionId: this.sessionStarted,
+        contextCallCount: this.contextCallCount,
+        expiryReason: 'context_low',
+      },
+    });
   }
 }
