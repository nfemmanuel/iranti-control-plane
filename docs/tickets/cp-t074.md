# CP-T074 — Submit CP-T025 Upstream PR

**Type:** Deliverable (not a feature)
**Priority:** P1
**Assigned:** system_architect
**Phase:** 5 (Wave 11)
**Status:** OPEN
**Epic:** CP-E003 (Staff Activity Stream)
**Depends on:** CP-T025 (spec PM-approved; diff files in `docs/specs/cp-t025-diffs/`)

---

## Description

CP-T025 (Native Staff Emitter Injection) was PM-accepted on 2026-03-20. The deliverable was:

1. A spec describing the changes needed in the Iranti upstream codebase to add native Staff event emission for the Attendant and Resolutionist components
2. An upstream PR description (`docs/specs/cp-t025-upstream-pr.md`)
3. Actual TypeScript diff files showing the specific code changes to be made

The spec and PR description are complete. The diff files were produced and are stored in `docs/specs/cp-t025-diffs/`. The PR has not been submitted.

**The gap:** Without this PR merged upstream, the Staff Activity Stream and Staff Logs show Librarian and Archivist events only. Attendant and Resolutionist events (including B11 attend classifier activity) are invisible. This has been the state since Phase 2. Phase 5 is the point to close it.

This ticket is a deliverable ticket — the system_architect must finalize and submit the PR to the Iranti GitHub repository.

---

## Context

- **Upstream repo:** `github.com/nfemmanuel/iranti`
- **Diff files location:** `docs/specs/cp-t025-diffs/` (in this repo)
- **PR description:** `docs/specs/cp-t025-upstream-pr.md`
- **Spec:** `docs/specs/cp-t025-*.md` (check for the main spec file)
- **What the PR adds:** Native Staff event emission from the Attendant and Resolutionist components — the same event schema that the Librarian and Archivist already use

---

## What to Do

### Step 1: Verify diff files are complete and current

Read all files in `docs/specs/cp-t025-diffs/` and confirm:
- The diff files cover all necessary changes to the Iranti upstream codebase
- The changes are consistent with Iranti v0.2.16's source structure (the repo has changed since the spec was written in Phase 2 — verify that the proposed changes still apply cleanly)
- If the Iranti source structure has changed (e.g., file renames, Staff component refactors), update the diff files to match the current Iranti codebase

**How to verify:** Compare the diff files against the actual Iranti source at `C:\Users\NF\Documents\Projects\iranti\src\staff\` (or equivalent location). If the target files have changed, produce updated diff content.

### Step 2: Create a fork or branch on the upstream repo

Navigate to `github.com/nfemmanuel/iranti`. Create a branch named `feat/cp-t025-staff-event-emission` from the current `main` branch.

### Step 3: Apply the diff files

Apply the changes from the diff files to the forked branch. The diff files contain TypeScript changes to the Attendant and Resolutionist components to emit Staff events in the same schema as Librarian and Archivist.

If applying the diffs requires judgment calls (e.g., merge conflicts with v0.2.16 changes), document the decisions in the PR description.

### Step 4: Submit the PR

Create a pull request from `feat/cp-t025-staff-event-emission` to `main` on `github.com/nfemmanuel/iranti`.

**PR title:** `feat(staff): native event emission for Attendant and Resolutionist`

**PR description:** Use the content from `docs/specs/cp-t025-upstream-pr.md` as the basis. Update it to reflect:
- The current Iranti version (v0.2.16) rather than whatever version was current when the spec was written
- Any changes made during Step 3 (merge conflict resolutions, file path updates, etc.)

### Step 5: Record the PR URL

Write the PR URL to Iranti shared memory and add it to this ticket. Update `docs/specs/cp-t025-upstream-pr.md` with the PR URL and submission date.

---

## Acceptance Criteria

### AC-1 — Diff files verified against v0.2.16

System architect has reviewed the diff files against the current Iranti v0.2.16 source structure and confirmed they apply cleanly, or has updated them to match.

### AC-2 — PR submitted

A pull request exists at `github.com/nfemmanuel/iranti` from branch `feat/cp-t025-staff-event-emission`. The PR title and description follow the spec.

### AC-3 — PR URL recorded

The PR URL is recorded in:
- This ticket (add a "PR URL" section below)
- `docs/specs/cp-t025-upstream-pr.md` (update with submission date and URL)
- Iranti shared memory (`ticket/CP-T025` entity)

### AC-4 — Known conflicts or blockers documented

If the diff cannot be applied cleanly due to v0.2.16 changes, the specific conflicts are documented in this ticket and the PR description, and the system architect has either resolved them or flagged them for upstream maintainer review.

### AC-5 — PM notified

PM is notified of PR submission so they can track upstream merge progress and decide when the Staff Activity Stream's coverage scope label (CP-T026) can be updated to reflect full 4-component coverage.

---

## PR URL

*(To be filled in by system_architect after submission)*

---

## Dependencies

- `docs/specs/cp-t025-diffs/` files exist and are complete (verify)
- `docs/specs/cp-t025-upstream-pr.md` exists and is complete (verify)
- GitHub access to `nfemmanuel/iranti`

## Owner

system_architect

## Priority

P1 — this has been an open carryover since Phase 2. Staff Activity Stream 4-component coverage is a Phase 2 outcome that was partially met (spec done, PR not submitted). Submitting the PR closes the deliverable.
